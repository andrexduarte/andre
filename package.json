{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host --tunnel",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "@nuxt/content": "^2.9.0",
    "@nuxt/image": "^1.1.0",
    "@nuxtjs/google-analytics": "^2.4.0",
    "nuxt": "^3.8.2",
    "nuxt-gtag": "^1.1.1",
    "push-dir": "^0.4.1",
    "sass": "^1.69.5"
  },
  "dependencies": {
    "untun": "^0.1.2",
    "vite-plugin-pug": "^0.3.2"
  }
}
